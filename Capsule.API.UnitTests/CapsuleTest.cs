using System;
using Xunit;
using Capsule.API.Models;

namespace WCapsule.API.UnitTests
{
    public class CapsuleTest
    {
        [Fact]
        public void SetInvalidDateTimeWhen()
        {
            // Arrange           
            //Capsule.API.Models.Capsule capsule = new Capsule.API.Models.Capsule();

            // Act & Assert
            //Assert.Throws<ArgumentException>(() => capsule.When = DateTime.Now.AddDays(-1));
        }
    }
}
